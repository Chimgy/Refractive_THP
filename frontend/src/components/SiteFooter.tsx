export default function SiteFooter() {
  return (
    <footer
      style={{
        marginTop: 'auto',
        borderTop: '1px solid var(--border)',
        padding: '20px 26px',
      }}
    />
  );
}
